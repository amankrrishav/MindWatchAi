# MindWatch Web

React + TypeScript frontend for MindWatch.

## Run

```bash
npm install
npm run dev
```

App: http://localhost:5173

## Config

Set `VITE_API_URL` in `.env` to point to the API (default: http://127.0.0.1:8000).
